services:
  ccs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ccs-dashboard
    restart: unless-stopped
    expose:
      - "3000"
    volumes:
      - ccs_:/root/.ccs
      - ./workspace:/workspace
    stdin_open: true
    tty: true

volumes:
  ccs_: {}