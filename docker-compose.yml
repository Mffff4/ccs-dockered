services:
  ccs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ccs-dashboard
    restart: unless-stopped
    ports:
      - "3003:3000"
    volumes:
      - ccs_data:/root/.ccs
      - ./workspace:/workspace
    stdin_open: true
    tty: true

volumes:
  ccs_data:
